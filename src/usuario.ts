// criar uma interface como o contrato, vai ser um molde do objeto 
//A interface define um molde para o objeto usuario

export interface Usuario{
    nome: string;
    email: string;
    idade: number;
    ativo: true;
}